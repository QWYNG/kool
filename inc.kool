
class Main {
  method Main() {
    var x = 0;
    var t1 = spawn { 
      for(var i=0; i<2; ++i) {
        
        x = x + 1;
        
      }
    };
    var t2 = spawn { 
      for(var i=0; i<2; ++i) {
        
        x = x + 1;
        
      }
    };

    join t1;
    join t2;

    print(x);
  }
}
